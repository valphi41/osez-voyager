{% extends 'Admin/layout.html.twig' %}

{% block content %}

    <h1>Modifier le lieu  "{{ europe.title }}"</h1>
    {% if errors %}
     <h4>Erreurs :</h4>
    <ul>
        {% for error in errors %}
            <li class="errors">{{ error }}</li>
        {% endfor %}
        </ul>
        {% endif %}
    {% include 'Admin/Europe/_form.html.twig' %}

    <a href="index">Back</a>

{% endblock %}
